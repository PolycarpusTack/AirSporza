# Planza — Development & Mitigation Backlog

_Date: 2026-06-12 · Generated by: backlog-builder (BB v5.1 / Core Spec v1) · Source: current-state evaluation (overall 4.9/10)_
_Repo: `C:\Projects\Planza` · Branch context: `feat/teams-repository-phase0` (Teams Phases 0–2 committed, applied to no DB)_

---

## 1. Readiness Decision

**Execution mode: DELIVERY** (declared). The architecture is validated (outbox, RLS, DLQs, idempotency, HMAC webhooks all in place); the work is mitigation and quality infrastructure on a live, feature-rich system. Note: several DELIVERY-mode preconditions (CI, fitness functions, written DoD, debt register, ADR set) do not yet exist — **EPIC A bootstraps them**. Until EPIC A completes, DELIVERY governance is applied to the backlog artifacts themselves; automated enforcement phases in as A-1/A-2 land.

**Solution Design Health Score: 8/9 — PROCEED**

| Dimension | Score | Rationale |
|---|---|---|
| Clarity | 3/3 | Evaluation names concrete files, line counts, and ordered impact; ambiguities are small and bounded. |
| Feasibility | 3/3 | All items use existing stack (GitHub Actions, Vitest, Prisma, Zod); no new vendors or unknown tech. |
| Completeness | 2/3 | This is an evaluation-derived mitigation plan, not a classic solution design: formal user journeys and data models are absent (acceptable for infra work), and the live-database inventory (how many DBs, which migrations applied where) is unknown — covered by A-2-T1 and the Assumptions Ledger. |

**High risks and mitigations (no unmitigated High → PROCEED holds):**

| Risk | Level | Mitigation |
|---|---|---|
| Migration baseline corrupts or diverges from the live DB state | High | A-2-T1 audits applied-vs-pending state and takes a verified `pg_dump` **before** any `migrate resolve`; baseline uses `prisma migrate diff` against the real DB, not the schema file alone. Rollback = restore dump. |
| `visibleByRoles` enforcement breaks existing clients that silently relied on seeing all fields | Medium | Feature flag `field_visibility_enforcement` default **off**; contract tests for both flag states; staged enablement. |
| Pagination is a breaking response-shape change | Medium | Envelope introduced behind flag / additive query params; default behavior preserved until frontend (B-4-T3) consumes it. |
| Solo developer + new CI = friction kills adoption | Medium | CI kept < 10 min, one workflow file, no required reviews; fitness functions added one at a time. |

**STRIDE highlights:** Information Disclosure is the active threat — `FieldDefinition.visibleByRoles` exists in schema (`backend/src/schemas/fieldConfig.ts:27`) but is enforced nowhere, so role-restricted fields are currently emitted to every authenticated role (B-1, highest-priority mitigation). CI introduces a new secrets surface (database URL, future API keys) — use GitHub Actions secrets, never `.env` in repo (covered in A-1-T3 quality gates). Tampering/Repudiation: migration apply path is currently manual `docker exec` with no record — A-2 replaces it with `prisma migrate` history (auditable `_prisma_migrations` table).

**Compliance audit:** No PII processing is added by this backlog. Team/player data is public sports data; tenant isolation via RLS already exists. Test data: use seeded synthetic tenants (existing `prisma/seed.ts`), no production dumps in CI.

---

## 2. Critical Gaps

1. **No database has the current branch's schema.** `Team` columns + `TeamCompetition` exist only in `schema.prisma`; `prisma db push` was never run (checkpoint doc). Any DB-backed test of Teams work is impossible until A-2-T3.
2. **Live-DB migration state is unverified.** `backend/prisma/migrations/` holds 31 flat `.sql` files + 1 `.ts` seed script applied manually via `docker exec`; `STATUS.md` lists possibly-unapplied migrations. The applied/pending matrix (A-2-T1) is the first information-generating task — its output can re-scope A-2.
3. **No CI of any kind** (`.github/workflows/` does not exist). Every quality property is currently enforced by memory.
4. **Package-manager ambiguity:** root `package.json` declares `"packageManager": "pnpm@10.30.3"` but only `package-lock.json` exists and all scripts use npm. CI must pick one — see Assumption ASM-2.
5. **Naming drift:** the npm packages are `sporza-planner` / `sporza-planner-backend`, the product is Planza, docs use both. Resolved by glossary decision below (code renames deferred to a TD item — not worth a breaking rename now).
6. **Frontend testing is a single file** (`src/pages/PlannerView.dnd.test.tsx`) with no Vitest config wired for the frontend workspace; "frontend tests in CI" requires the harness first (A-1-T2).
7. **Cascade semantic divergence is documented but unresolved:** `backend/tests/cascade.test.ts` covers `compute.ts`/`estimator.ts` and records a divergence with the `schedules.ts` cascade preview; the orchestrator `engine.ts` (203 lines) has zero tests (B-2).

---

## 3. Domain Glossary

Enforced from this backlog onward (DELIVERY): one term per concept, one concept per term — in code, prompts, and docs.

| Term | Definition |
|---|---|
| **Planza** | The product and system name. Use everywhere going forward. |
| ~~Sporza Planner / SporzaPlanner / sporza-planner~~ | **Deprecated synonyms** of Planza. Survive only in npm package names and legacy identifiers; renaming is TD-10, not in scope. Never introduce in new code or docs. |
| **Tenant** | An isolated customer organisation; enforced via Postgres RLS on `tenantId`. |
| **Canonical Team** | The provider-agnostic deduplicated team record produced by the import pipeline (`CanonicalTeam`); source of truth for cross-provider identity. |
| **Team** | The operational, tenant-owned team row used by planning features; may be bridged from a Canonical Team via `canonicalTeamId` (Option B bridge, Teams Phase 1). |
| **Managed Team** | A Team with `isManaged = true`: manual edits are protected against import overwrites via cross-source field priority. |
| **Team Membership** | A `TeamCompetition` row linking a Team to a Competition (optionally per season). UI verb is "assign to league"; the noun in code is always *membership* — never "assignment". |
| **Squad** | UI label for the `/teams` repository page. UI-only term; code says Team. |
| **Player** | (Phase 3, future) An athlete; `Player` + `PlayerTeam` models. Not "athlete" in code. |
| **Import Source** | A configured external data provider instance (e.g. `the_sports_db`) with credentials and base URL. |
| **Import Job** | One execution of the import pipeline for an Import Source + entity scope, run by `ImportJobRunner` through fetch → normalize → dedupe → merge → provision stages. |
| **Source Link** | The record tying a canonical entity to its identifier at a specific Import Source. |
| **Outbox** | The transactional outbox table + trigger; the only path from API writes to async workers/webhooks. |
| **Cascade** | The schedule-recomputation pipeline (`services/cascade/`: `compute.ts`, `estimator.ts`, `engine.ts`, `alerts.ts`) that propagates event changes. *Cascade preview* = the read-only what-if in `schedules.ts`. |
| **Field Definition** | A tenant-configurable dynamic field (`fieldConfig.ts`); **Field Visibility** = its `visibleByRoles[]` restriction. Empty array = visible to all roles (ASM-5). |
| **Quality Loop** | The CI pipeline: typecheck + lint + tests (both workspaces) + migrations against a disposable Postgres. |
| **Migration Baseline** | The single generated migration representing the current live schema, after which `prisma migrate` owns history. |

---

## 4. Assumptions Ledger

| ID | Assumption | Impact | If wrong |
|---|---|---|---|
| **ASM-1** | The repo is hosted on GitHub (or will be) and GitHub Actions is available. | **HIGH** | A-1-T3/A-2-T4 re-target to another CI (GitLab CI/Forgejo); workflow logic is portable shell, so cost is bounded. |
| **ASM-2** | **npm is the package manager of record** (only `package-lock.json` exists); the `"packageManager": "pnpm@10.30.3"` field is stale. CI uses `npm ci`. | **HIGH** | If pnpm is intended, swap install steps and commit `pnpm-lock.yaml`; decide in A-1-T1 (Pull Gate checks this). |
| **ASM-3** | There is exactly one live database environment (docker-compose Postgres) whose state A-2-T1 must audit; no separately-drifted staging/prod pair. | **HIGH** | A-2-T1 repeats the audit per environment; `migrate resolve` baseline applied per DB. |
| **ASM-4** | A recent, restorable backup can be taken (`pg_dump`) before baselining. | **HIGH** | A-2 is **HOLD** until backup capability exists — do not baseline without rollback. |
| **ASM-5** | `visibleByRoles: []` (the schema default) means "visible to all roles", and admin roles always see all fields. | Med | Invert filter logic in B-1-T2; contract tests make the chosen semantics explicit either way. |
| **ASM-6** | Backend Vitest suites currently pass without a live database (mocked Prisma); "runs against a database" is a DoD addition, not a current property. | Med | If suites need a DB, A-1-T3 adds the Postgres service container earlier (merging part of A-2-T4 into it). |
| **ASM-7** | The Planza frontend is the only consumer of the REST API (no external API clients), so pagination envelopes and field filtering are coordinated releases, not public breaking changes. | Med | Add API versioning/deprecation window to B-4; raise EPIC F (OpenAPI) priority. |
| **ASM-8** | RLS and all raw-SQL migrations run on the vanilla `postgres:16` image (no managed-Postgres-specific SQL), so a CI service container is representative. | Med | Pin CI image to match production version/extensions. |
| **ASM-9** | Solo developer: CI triggers on every push to every branch; no required PR reviews; branch protection optional. | Low | Tighten when team grows. |
| **ASM-10** | The evaluation's ~10 seed TD items and ~53 `any` instances are taken as given; A-3-T3 re-verifies counts against the code rather than re-running the evaluation. | Low | Debt register is a living doc; correct on first servicing pass. |

---

## 5. Backlog

**Conventions:** Branch `feature/[STORY-ID]-short-description` · Commits `[type]([scope]): [summary]` · IDs per BB §6. Sizes are relative (S/M/L/XL), never mapped to days (Core P9). Model tiers per Core §6.

**Solo-dev calibration (Core §5.1):** governance is kept at DELIVERY level for anything touching the database, security (field visibility), or CI (high blast radius / hard to reverse); documentation tasks carry minimal ceremony (their spec ≈ their output; anti-bureaucracy test).

---

## EPIC A — Quality Loop Tracer Bullet & Governance Baseline

- **Objective:** Stand up the thinnest end-to-end quality loop — CI that typechecks, lints, and tests both workspaces, runs one new frontend smoke test, and applies the consolidated migration history to a disposable Postgres — plus the written governance artifacts that make DELIVERY mode real.
- **Tracer Bullet?:** **YES.** The tracer bullet here is not a feature slice but the thinnest slice of the *new quality infrastructure*, proving the whole loop (commit → CI → typecheck/lint/test/migrate → green) end-to-end. **This EPIC bootstraps DELIVERY mode's own preconditions** (Core §1: fitness functions in CI, full DoD, ADR set, debt tracking); until it lands, those preconditions are satisfied manually.
- **Mode:** DELIVERY
- **Definition of Done (additions to Global DoD):**
  1. A push to any branch produces a CI run that typechecks (both workspaces), lints, runs backend + frontend Vitest, and `prisma migrate deploy`s the full history onto a disposable Postgres — all green on `feat/teams-repository-phase0` and `main`.
  2. `prisma migrate status` reports a clean, fully-applied history against the live database; the manual `docker exec` apply path is retired in writing.
  3. DoD doc (including "runs against a database"), Domain Glossary, Architecture Memory, debt register (≥ 10 seeded items), and 5 backfilled ADRs exist under `docs/governance/`.
- **Business Value:** High — every later EPIC's safety net; converts "possibly-unapplied migrations" from a standing production risk into a verified, automated property. Success metric: 0 manual migration applies after A-2; CI catches ≥ 1 real defect within the first month (leading indicator that the loop has teeth).
- **Risk Assessment:** Migration baseline vs live DB — High, mitigated (audit + backup first, A-2-T1; rollback = restore). CI flakiness from DB-dependent tests — Med, mitigated (service container with health checks, ASM-8). All others Low.
- **SLO Definitions:** `CI – pipeline duration < 10 min over last 20 runs` · `CI – pass rate on main ≥ 95% over 30 days` · `Migrations – drift between schema.prisma and live DB = 0 (checked every CI run via migrate status)`.
- **Domain Glossary (this EPIC):** Quality Loop, Migration Baseline (see §3).
- **Assumptions:** ASM-1, ASM-2, ASM-3, ASM-4 (all HIGH — verify at A-1-T1 / A-2-T1 pull gates).
- **ADRs:** produces ADR-001…ADR-005 (backfill, A-3-T2) + ADR-006 (CI platform & package manager, A-1-T3) + ADR-007 (migration strategy, A-2-T2).
- **Smoke Test Story:** A-1 (the CI run itself + `eventReadiness` frontend smoke test is the E2E smoke for this EPIC).
- **Runbook Link:** `docs/governance/runbook-ci-and-migrations.md` (created in A-2-T4): symptoms (red CI, migrate drift), checks, logs, rollback.

---

### STORY A-1 — CI quality loop (tracer bullet spine)

- **Persona Narrative:** As the **solo Planza developer**, I want every push to automatically typecheck, lint, and test both workspaces so that regressions are caught by a machine instead of by memory, before any further mitigation work begins.
- **Business Value:** High (3) · **Priority Score: 5/5** (max dependency impact — everything else pulls from this).
- **Acceptance Criteria:**
  - Given a push to any branch, When CI runs, Then `tsc --noEmit` (backend, from `backend/`), `tsc -b --force` (frontend), `eslint` (both), `vitest run` (backend), and `vitest run` (frontend, ≥ 1 smoke test) all execute and the run is green on the current branch head.
  - Given a commit that breaks any of those checks, When CI runs, Then the run fails with the failing step identifiable in < 1 minute of log reading.
  - Given a backend service file importing from `routes/`, When CI runs, Then the dependency-direction check fails the build (error flow).
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** M
- **External Dependencies:** GitHub Actions availability (ASM-1).
- **TD Considerations:** Frontend coverage threshold deliberately not set yet (principal: small; interest: none until EPIC B expands tests) — TD-11 recorded at A-3-T3.
- **Test Data Requirements:** none beyond existing fixtures.
- **Idempotency:** n/a (no write paths).
- **DoR Status: READY**

#### TASK A-1-T1 — Baseline the local quality loop
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** High
- **Goal:** Run the full quality loop locally exactly as CI will (`npm ci` both workspaces; backend `npx tsc --noEmit` from `backend/`; frontend `npx tsc -b --force`; `npm run lint` root + backend; `vitest run` backend) and record/fix every breakage so A-1-T3 automates a *known-green* loop.
- **Token/Size Budget:** ≤ 8k (mostly command output triage; small fixes only — anything structural becomes its own TD item, not fixed here).
- **TDD Order:** n/a (verification task; existing tests are the spec).
- **Required Interfaces:** root + backend `package.json` scripts (verified present).
- **Deliverables:** short `docs/governance/local-quality-loop.md` (the exact command list CI will mirror); lint/type fixes ≤ 50 LOC each; TD items for anything bigger.
- **Quality Gates:** all five commands exit 0 locally; **decide ASM-2** (npm vs pnpm) and remove or honor the stale `packageManager` field.
- **Rollback Notes:** doc + small fixes; revert commits.
- **Hand-Off Artifacts:** the verified command list (input contract for A-1-T3).
- **Pull Gate:** confirm checkpoint doc's known issues (stale `tsconfig.tsbuildinfo`, backend typecheck must run from `backend/`) still reproduce; confirm which lockfile is authoritative (ASM-2).
- **Unblocks:** A-1-T2, A-1-T3.

#### TASK A-1-T2 — Frontend test harness + first smoke test
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Wire Vitest + React Testing Library into the frontend workspace (vitest config, jsdom env, test script in root `package.json`) and add one smoke test for `src/utils/eventReadiness.ts` proving the harness runs headless.
- **Token/Size Budget:** ≤ 6k.
- **TDD Order:** (1) write the failing `eventReadiness` smoke test (3–5 cases on the pure function), (2) wire config until it runs and passes, (3) refactor config to share TS paths with `vite.config`.
- **Required Interfaces:** `eventReadiness.ts` public exports (read the file; it is pure — no mocks needed).
- **Abstraction Check:** ensure the existing `src/pages/PlannerView.dnd.test.tsx` runs under the same config — do not create a second harness (Rule of One harness).
- **Deliverables:** `vitest.config.ts` (frontend), `src/utils/eventReadiness.test.ts`, `"test"` script at root.
- **Quality Gates:** `npm run test` (root) exits 0; the pre-existing dnd test is collected and passes or is explicitly skipped with a TD item.
- **Feature Flag:** n/a (dev tooling).
- **Rollback Notes:** delete config + test; zero runtime impact.
- **Hand-Off Artifacts:** Contract Snapshot `FrontendTestHarness` (how to add a test: file pattern, env, setup file) — consumed by B-3.
- **Pull Gate:** A-1-T1's command list is green; Vitest major version pinned to backend's (v3) to keep one mental model (upgrade is EPIC E).
- **Unblocks:** A-1-T3, B-3-T1.

#### TASK A-1-T3 — GitHub Actions workflow
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Create `.github/workflows/ci.yml` running on every push: checkout → setup-node (cache npm) → `npm ci` (root + backend) → backend typecheck → frontend typecheck → lint (both) → backend vitest → frontend vitest.
- **Token/Size Budget:** ≤ 5k (one YAML file + ADR).
- **TDD Order:** the "failing test" is a deliberately broken push (or `act` dry-run) proving the pipeline fails red before the green commit. Then green. Then refactor into named jobs.
- **Required Interfaces:** A-1-T1 command list (hand-off artifact).
- **Deliverables:** `ci.yml`; `ADR-006: CI platform = GitHub Actions; package manager = npm` (one page).
- **Quality Gates:** green run on the branch; total duration < 10 min (SLO); no secrets in the workflow file.
- **Rollback Notes:** delete workflow file.
- **Hand-Off Artifacts:** workflow job names (extension points for A-1-T4 and A-2-T4).
- **Pull Gate:** ASM-1 confirmed (repo on GitHub with Actions enabled); ASM-2 decision from A-1-T1 honored.
- **Unblocks:** A-1-T4, A-2-T4.

#### TASK A-1-T4 — Dependency-direction fitness function
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Add the first architectural fitness function: a check (eslint `no-restricted-imports` config or a small script) failing CI when any file under `backend/src/services/**` or `backend/src/import/**` imports from `backend/src/routes/**`.
- **Token/Size Budget:** ≤ 4k.
- **TDD Order:** (1) add a temporary violating import in a scratch branch/test fixture and prove the check fails, (2) remove it, check passes, (3) wire into `ci.yml` as its own step.
- **Required Interfaces:** A-1-T3 workflow job names.
- **Abstraction Check:** prefer extending the existing eslint config over a bespoke script (reuse before build).
- **Deliverables:** lint rule/script + CI step + 3-line note in Architecture Memory ("fitness functions" section).
- **Quality Gates:** CI red on violation, green on current codebase (if current code already violates, record TD item and scope the rule to non-violating dirs rather than blocking — visible debt over silent exception).
- **Rollback Notes:** remove rule/step.
- **Hand-Off Artifacts:** pattern for adding further fitness functions (EPIC D/E reuse).
- **Pull Gate:** A-1-T3 pipeline green.
- **Unblocks:** END OF STORY SEQUENCE (and pattern reuse by later EPICs).

---

### STORY A-2 — Migration consolidation onto `prisma migrate`

- **Persona Narrative:** As the **solo Planza developer**, I want the database schema history owned by `prisma migrate` with a verified baseline so that "is this migration applied?" is answered by a command instead of archaeology, and the Teams schema can finally reach a database safely.
- **Business Value:** High (3) · **Priority Score: 5/5** (gates Teams Phase 3, Prisma upgrade, and all DB-backed testing).
- **Acceptance Criteria:**
  - Given the live database, When `npx prisma migrate status` runs, Then it reports all migrations applied and no drift.
  - Given a disposable empty Postgres, When `npx prisma migrate deploy` runs, Then the full schema (including `Team` columns + `TeamCompetition`) is created and the backend test suite can run against it.
  - Given a teammate (or future me) needing to apply schema changes, When they read the runbook, Then the only documented path is `prisma migrate` — the `docker exec` path is marked retired (error flow: runbook says what to do if `migrate status` reports drift).
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** L
- **External Dependencies:** live Postgres access + backup capability (ASM-3, ASM-4 — **story is HOLD if ASM-4 fails**).
- **TD Considerations:** the 31 historical raw-SQL files remain in-repo as `migrations_legacy/` for forensics (principal: tiny; interest: none) — recorded as part of ADR-007, not a TD item.
- **Test Data Requirements:** disposable Postgres seeded via existing `prisma/seed.ts`.
- **Idempotency:** `prisma migrate deploy` is idempotent by design; `migrate resolve` steps are recorded one-by-one in the runbook as executed.
- **DoR Status: READY** (conditional on ASM-4 at A-2-T1 pull gate)

#### TASK A-2-T1 — Audit applied vs pending migrations
- **Hat:** PREPARATORY · **Model: Opus** (judgment: interpreting drift, deciding what is truth) · **Confidence:** Medium
- **Goal:** Produce the applied/pending matrix: for each of the 31 raw SQL files + `STATUS.md` claims, determine via live-DB introspection (`prisma db pull` to a scratch schema file, diffed against `schema.prisma`; targeted `information_schema`/`pg_policies` queries for RLS, triggers, indexes) whether it is applied. Take and verify a `pg_dump` backup first.
- **Token/Size Budget:** ≤ 10k.
- **TDD Order:** n/a (audit; the matrix is the deliverable). Verification = the diff between `db pull` output and `schema.prisma` is fully explained line-by-line by the matrix.
- **Required Interfaces:** live `DATABASE_URL`; `backend/prisma/STATUS.md`.
- **Deliverables:** `docs/governance/migration-audit-2026-06.md` (matrix: file → applied? → evidence); verified backup noted with location + restore command.
- **Quality Gates:** every unexplained diff line either gets applied in A-2-T2 scope or a TD item; backup restore tested on a scratch container.
- **Rollback Notes:** read-only task (plus backup).
- **Hand-Off Artifacts:** the matrix (input contract for A-2-T2).
- **Pull Gate:** **ASM-3** (how many live environments?) and **ASM-4** (backup possible?) verified before touching anything. If ASM-4 fails → story HOLD.
- **Unblocks:** A-2-T2.

#### TASK A-2-T2 — Baseline schema onto `prisma migrate`
- **Hat:** FEATURE · **Model: Opus** (irreversible decision execution) · **Confidence:** Medium
- **Goal:** Apply any truly-pending legacy SQL from the A-2-T1 matrix; then create the baseline: move legacy files to `prisma/migrations_legacy/`, generate `migrations/0_init/migration.sql` via `prisma migrate diff --from-empty --to-schema-datamodel` **excluding the not-yet-applied Team/TeamCompetition delta**, and `prisma migrate resolve --applied 0_init` against the live DB.
- **Token/Size Budget:** ≤ 12k.
- **TDD Order:** (1) executable check first: a script/test that runs `migrate deploy` on a disposable container and asserts `migrate status` clean + key tables/policies exist (fails before baseline exists); (2) create baseline until it passes; (3) tidy file layout.
- **Required Interfaces:** A-2-T1 matrix; ASM-8 (vanilla postgres image is representative).
- **Deliverables:** `0_init` migration; `migrations_legacy/` move; `ADR-007: Migration strategy — baseline + prisma migrate ownership; db:push retired for shared DBs` ; verification script.
- **Quality Gates:** `migrate status` clean on live DB **and** `migrate deploy` + backend tests green on a disposable container; `npm run db:push` script annotated as local-prototyping-only.
- **Rollback Notes:** baseline is metadata-only on the live DB (`migrate resolve` writes `_prisma_migrations` rows); rollback = delete those rows + restore file layout. Schema-changing legacy applies (if any) roll back via the A-2-T1 backup.
- **Hand-Off Artifacts:** Contract Snapshot `MigrationWorkflow` (commands for create/apply/verify) — consumed by every future schema change including EPIC G.
- **Pull Gate:** A-2-T1 matrix complete; backup verified restorable.
- **Unblocks:** A-2-T3.

#### TASK A-2-T3 — Team/TeamCompetition migration (first real migration)
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Generate the additive migration for the Teams Phases 0–2 schema (`Team.sportId/canonicalTeamId/notes/isManaged`, `TeamCompetition` + back-relations) with `prisma migrate dev`, apply to the live DB, and run the existing teams routes against it (manual smoke: `/teams` page loads, `PATCH /:id/notes` works).
- **Token/Size Budget:** ≤ 6k (migration is generated; work is verification).
- **TDD Order:** (1) extend the A-2-T2 verification script to assert `TeamCompetition` exists + `Team` columns present (fails); (2) generate + deploy migration (passes); (3) none.
- **Required Interfaces:** `MigrationWorkflow` snapshot; current `schema.prisma` (already contains the models — committed in `aa51e84`/`7b5b4c5`).
- **Deliverables:** `migrations/<ts>_add_team_repository/` ; checkpoint doc updated ("applied to DB" ✅).
- **Quality Gates:** migration is purely additive (no data migration needed — confirmed by checkpoint doc); `migrate status` clean; known caveat re-verified: `TeamCompetition` NULL-season uniqueness is route-enforced (existing TD, carried in register).
- **Feature Flag:** not needed — schema is additive and the `/teams` UI already shipped behind nav visibility.
- **Rollback Notes:** documented down-SQL in the runbook (drop `TeamCompetition`, drop added columns); additive → safe.
- **Hand-Off Artifacts:** confirmation that DB-backed Teams testing is now possible (gate for EPIC G).
- **Pull Gate:** A-2-T2 `migrate status` clean on live DB.
- **Unblocks:** A-2-T4; EPIC G (Teams Phase 3) DB precondition.

#### TASK A-2-T4 — Migration step in CI + apply-path runbook
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Extend `ci.yml` with a `postgres:16` service container; run `prisma migrate deploy` + `prisma migrate status` + backend tests with `DATABASE_URL` pointing at it (ASM-6: if suites are mock-only today, add one DB-backed smoke spec exercising a real Prisma query through RLS). Write the runbook.
- **Token/Size Budget:** ≤ 8k.
- **TDD Order:** (1) push with a deliberately missing migration → CI red; (2) fix → green; (3) factor DB steps into a reusable job.
- **Required Interfaces:** A-1-T3 workflow; `MigrationWorkflow` snapshot.
- **Deliverables:** updated `ci.yml`; one DB-backed smoke test (`backend/tests/db-smoke.test.ts`); `docs/governance/runbook-ci-and-migrations.md` (symptoms/checks/rollback; `docker exec` path marked **retired**).
- **Quality Gates:** full pipeline still < 10 min; DoD item "runs against a database" now machine-enforced. **Tracer bullet complete at this task** — the entire quality loop is proven end-to-end.
- **Rollback Notes:** remove service container block.
- **Hand-Off Artifacts:** DB-in-CI pattern (reused by B-1/B-4 contract tests and EPIC G).
- **Pull Gate:** A-2-T3 applied; A-1-T3 pipeline green; ASM-8 holds.
- **Unblocks:** END OF STORY SEQUENCE; B-1-T2, B-4-T1 (DB-backed contract tests in CI).

---

### STORY A-3 — Governance artifacts

- **Persona Narrative:** As the **solo Planza developer (and any future AI agent working on Planza)**, I want the DoD, glossary, ADRs, Architecture Memory, and debt register written down so that decisions and debt survive context loss between sessions.
- **Business Value:** Med (2) · **Priority Score: 4/5** (cheap, unblocks consistent execution of everything else).
- **Acceptance Criteria:**
  - Given `docs/governance/`, When reviewed, Then it contains: DoD (with "runs against a database"), Domain Glossary (§3 of this doc, extracted), Architecture Memory (Core §4.2 format), debt register with ≥ 10 items in TD format, and ADR-001…005.
  - Given any new task in this backlog, When executed, Then its TD/glossary references resolve to these files (no dangling references).
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** S
- **External Dependencies:** evaluation report (source of the ~10 TD items).
- **Test Data / Idempotency:** n/a (documentation).
- **DoR Status: READY**

#### TASK A-3-T1 — DoD + Domain Glossary documents
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** High
- **Goal:** Write `docs/governance/definition-of-done.md` (Global DoD from Core §3, solo-calibrated, plus "new logic runs against a database in CI") and `docs/governance/domain-glossary.md` (§3 above, including the Planza-naming decision and Teams vocabulary).
- **Budget:** ≤ 4k · **TDD Order:** n/a (docs) · **Deliverables:** the two files · **Quality Gates:** glossary terms match this backlog's usage exactly; spec-vs-output anti-bureaucracy check passes trivially (the doc *is* the output).
- **Pull Gate:** none (root task — may run in parallel with A-1) · **Unblocks:** A-3-T2.

#### TASK A-3-T2 — ADR backfill + Architecture Memory
- **Hat:** PREPARATORY · **Model: Opus** (reconstructing decision context and consequences) · **Confidence:** High
- **Goal:** Backfill five one-page ADRs for irreversible decisions already made — ADR-001 transactional outbox, ADR-002 Postgres RLS multi-tenancy, ADR-003 BullMQ + DLQs, ADR-004 raw-SQL migration history (superseded-by: ADR-007), ADR-005 JWT/Passport RBAC — and write `docs/governance/architecture-memory.md` (Core §4.2: components, ADR index, glossary pointer, integration map, active TD, current mode = DELIVERY).
- **Budget:** ≤ 10k · **TDD Order:** n/a · **Required Interfaces:** read-only code spelunking (`backend/src/`) for accurate context sections.
- **Deliverables:** `docs/governance/adr/ADR-001..005.md`, `architecture-memory.md` · **Quality Gates:** each ADR has context/alternatives/consequences/review-date; Architecture Memory ≤ ~500 tokens (Core §4.4 budget).
- **Pull Gate:** A-3-T1 glossary exists (ADRs must use its terms) · **Unblocks:** A-3-T3.

#### TASK A-3-T3 — Seed the debt register
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** High
- **Goal:** Create `docs/governance/debt-register.md` seeding ~10 TD items from the evaluation in full format (artifact, type, cause, principal, interest, compounding, servicing decision, origin): expected set — TD-1 `ImportJobRunner.ts` god file (1660 ln), TD-2 `routes/import.ts` (1352 ln), TD-3 `PlannerView.tsx` (1009 ln), TD-4 `AdminView.tsx` (793 ln), TD-5 cascade `engine.ts` untested + preview divergence, TD-6 `visibleByRoles` unenforced (closes with B-1), TD-7 unpaginated list endpoints (closes with B-4), TD-8 ~53 `any` instances, TD-9 `TeamCompetition` NULL-season route-only uniqueness, TD-10 sporza-planner package naming drift, TD-11 no frontend coverage threshold.
- **Budget:** ≤ 6k · **TDD Order:** n/a · **Quality Gates:** every item has a servicing decision (pay-down EPIC reference, accept-with-date, or live-with-it rationale); counts re-verified against code (ASM-10).
- **Pull Gate:** A-3-T2 Architecture Memory exists (register linked from it) · **Unblocks:** END OF STORY SEQUENCE; referenced by every later EPIC.

---

## EPIC B — High-Value Mitigations (security, correctness, scale)

- **Objective:** Close the four highest-impact gaps from the evaluation: enforce field-level role visibility, characterize and reconcile the cascade engine, expand the frontend test foundation, and paginate the highest-volume list endpoints.
- **Tracer Bullet?:** NO (adds depth on the EPIC A loop).
- **Mode:** DELIVERY
- **Definition of Done (additions to Global DoD):**
  1. `visibleByRoles` is enforced on every dynamic-field-bearing API response, with contract tests per role in CI, behind a flag that has been turned on.
  2. `cascade/engine.ts` has characterization coverage and the engine-vs-preview divergence is either reconciled or accepted in an ADR.
  3. `/api/events`, `/api/teams`, and import-record listings are paginated with tests; the frontend loads incrementally without functional regression.
- **Business Value:** High — B-1 closes an information-disclosure hole; B-4 removes the biggest scalability cliff; B-2/B-3 convert the two scariest untested areas into safety-netted code. Success metric: 0 fields visible to unauthorized roles in contract tests; `/api/events` p95 stays flat as event count grows 10×.
- **Risk Assessment:** B-1 breaking hidden client reliance — Med, mitigated (flag default off + both-states contract tests). B-2 characterization may reveal the preview or the engine is *wrong*, not just divergent — Med, mitigated (decision task B-2-T2 with ADR; behavior change only under its own flagged follow-up). B-4 envelope break — Med, mitigated (additive params, frontend updated in same EPIC).
- **SLO Definitions:** `API /api/events – p95 latency < 400ms over 7 days at 10k events` · `API – unauthorized field disclosure = 0 (contract-test enforced per CI run)`.
- **Domain Glossary (this EPIC):** Field Definition, Field Visibility, Cascade, Cascade Preview (see §3).
- **Assumptions:** ASM-5, ASM-6, ASM-7.
- **ADRs:** ADR-008 (cascade semantics decision, B-2-T2); ADR-009 (pagination convention, B-4-T1).
- **Smoke Test Story:** B-4-T3 includes an E2E smoke (frontend boots against paginated API, planner renders first page + loads more).
- **Runbook Link:** `docs/governance/runbook-api.md` additions: field-visibility flag operation, pagination defaults, cascade divergence symptoms.

---

### STORY B-1 — Enforce `visibleByRoles` on API responses

- **Persona Narrative:** As a **tenant administrator**, I want fields marked visible-by-role to actually be withheld from other roles' API responses so that role-restricted information (the promise the admin UI already makes) is real.
- **Business Value:** High (3) · **Priority Score: 5/5** (security, Core Domain, existing harness lowers cost).
- **Acceptance Criteria:**
  - Given a FieldDefinition with `visibleByRoles: ['admin']` and the flag ON, When a `sports` user fetches an event/team carrying that field, Then the field is absent from the response; When an `admin` fetches it, Then it is present.
  - Given `visibleByRoles: []`, When any role fetches, Then the field is present (ASM-5 semantics, made explicit).
  - Given the flag OFF, When any role fetches, Then responses are byte-identical to today (regression guard).
  - Error flow: Given a FieldDefinition referencing an unknown role, When responses are shaped, Then the field is treated as restricted (fail-closed) and a warning is logged.
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** M
- **External Dependencies:** none (harness exists: `backend/tests/contracts-rbac.test.ts`).
- **TD Considerations:** closes TD-6.
- **Test Data:** seeded tenant with one restricted + one open FieldDefinition per entity type.
- **Idempotency:** n/a (read-path shaping).
- **DoR Status: READY**

#### TASK B-1-T1 — Inventory field-bearing responses + visibility filter contract
- **Hat:** PREPARATORY · **Model: Opus** (judgment: where shaping must hook in without N response-path forks) · **Confidence:** Medium
- **Goal:** Grep/trace every route emitting FieldDefinition-governed payloads (events, teams, any `customFields`/dynamic-field serialization) and define the single choke-point contract `filterFieldsByRole(entityPayload, fieldDefs, userRoles): payload` — including the fail-closed rule and `[] = public` rule.
- **Budget:** ≤ 6k · **TDD Order:** n/a (analysis) — but deliverable includes the *test list* (10–15 named cases) B-1-T2 will write first.
- **Deliverables:** Contract Snapshot `FieldVisibilityFilter` (signature, error shape, glossary terms) + route inventory in the task notes.
- **Quality Gates:** inventory is exhaustive (cross-checked against `fieldConfig.ts` consumers); no route requires bespoke logic — if one does, raise scope before T2.
- **Pull Gate:** ASM-5 confirmed against admin-UI behavior (what does the UI *promise* for empty array?).
- **Unblocks:** B-1-T2.

#### TASK B-1-T2 — Implement enforcement with contract tests
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Implement `FieldVisibilityFilter` and apply it at the inventoried choke points, gated by env/app-setting flag `FIELD_VISIBILITY_ENFORCEMENT` (default **off**).
- **Budget:** ≤ 12k · **TDD Order:** (1) write the B-1-T1 test list into `contracts-rbac.test.ts` extensions — per-role, per-flag-state, fail-closed — all failing; (2) simplest filter passing; (3) refactor shaping into the choke point (no per-route duplication — Rule of Three pre-empted by design).
- **Required Interfaces:** `FieldVisibilityFilter` snapshot; existing RBAC harness; DB-in-CI pattern (A-2-T4).
- **Deliverables:** filter module + tests → route wiring → flag documentation in runbook.
- **Quality Gates:** flag-off responses byte-identical (snapshot assertion); ≥ 80% coverage on the filter; warning log on unknown role.
- **Feature Flag:** `FIELD_VISIBILITY_ENFORCEMENT`, default off; enablement note in runbook (turn on → watch logs for fail-closed warnings → fix defs → leave on).
- **Rollback Notes:** flag off restores exact prior behavior.
- **Hand-Off Artifacts:** updated `FieldVisibilityFilter` snapshot v2 (as built).
- **Pull Gate:** A-2-T4 DB-in-CI green (contract tests need real FieldDefinitions); B-1-T1 inventory unchanged by intervening commits.
- **Unblocks:** END OF STORY SEQUENCE. TD-6 closed on flag-on.

---

### STORY B-2 — Cascade engine characterization & reconciliation

- **Persona Narrative:** As the **solo Planza developer**, I want `cascade/engine.ts` pinned by characterization tests and the engine-vs-preview divergence resolved or formally accepted so that schedule recomputation can be changed without fear.
- **Business Value:** High (3) · **Priority Score: 4/5**.
- **Acceptance Criteria:**
  - Given the current engine behavior, When the characterization suite runs, Then it pins orchestration ordering, error propagation, alert emission, and outbox interaction for ≥ the main path + 2 failure paths.
  - Given the documented divergence between `engine.ts` semantics and the `schedules.ts` cascade preview, When B-2-T2 completes, Then either both code paths agree (covered by a shared test) or ADR-008 records the divergence as accepted with rationale and a UI-facing consequence note.
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** M
- **External Dependencies:** none.
- **TD Considerations:** services TD-5.
- **Test Data:** existing cascade fixtures from `cascade.test.ts`, extended.
- **Idempotency:** engine writes go through existing outbox — characterization must pin idempotency-key behavior as-is.
- **DoR Status: READY**

#### TASK B-2-T1 — Characterization tests for `engine.ts`
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** Medium (203 lines but orchestration: mocking surface may be wide)
- **Goal:** Write golden-master characterization tests for `backend/src/services/cascade/engine.ts`: capture *current* behavior (even where suspected wrong) for the happy path, a compute failure, and a partial-application failure — no production code changes.
- **Budget:** ≤ 10k · **TDD Order:** characterization variant — tests written to *pass against current behavior*; any surprising behavior is recorded as a finding, not fixed (One Hat: no behavior change in this task).
- **Required Interfaces:** `engine.ts` exports; existing `cascade.test.ts` patterns and its documented divergence notes.
- **Deliverables:** `backend/tests/cascade-engine.test.ts` + findings list (input to B-2-T2).
- **Quality Gates:** engine orchestration paths covered; suite green in CI; zero diffs to `src/`.
- **Rollback Notes:** test-only.
- **Hand-Off Artifacts:** findings list with exact divergence reproduction.
- **Pull Gate:** verify the divergence note in `cascade.test.ts` still reflects current `schedules.ts` code.
- **Unblocks:** B-2-T2.

#### TASK B-2-T2 — Reconcile or document engine-vs-preview divergence
- **Hat:** FEATURE (if reconciled — behavior change) or terminates as documentation · **Model: Opus** (semantic judgment with planner-facing consequences) · **Confidence:** Low → structured as decision-first
- **Goal:** Using B-2-T1 findings, decide which semantics are correct (engine, preview, or neither), write `ADR-008: Cascade semantics`; if reconciliation is chosen, implement it behind flag `CASCADE_PREVIEW_PARITY` with the characterization suite updated *deliberately* (each changed expectation justified in the ADR).
- **Budget:** ≤ 12k; if reconciliation exceeds this, the implementation splits into a follow-up task and this task ships ADR-only.
- **TDD Order:** (1) write the *desired-semantics* test (failing if reconciling); (2) minimal change to engine or preview; (3) refactor shared logic only if both paths now duplicate it (Rule of Three: second occurrence → extract is allowed).
- **Required Interfaces:** B-2-T1 findings; `compute.ts`/`estimator.ts` contracts (already tested).
- **Deliverables:** ADR-008 → (conditional) reconciliation diff + updated tests.
- **Quality Gates:** characterization suite changes are each ADR-justified; flag-off preserves current behavior.
- **Feature Flag:** `CASCADE_PREVIEW_PARITY` (only if behavior changes), default off.
- **Rollback Notes:** flag off; ADR records the rollback trigger (planner-visible miscalculation reports).
- **Hand-Off Artifacts:** ADR-008; updated cascade Contract Snapshot.
- **Pull Gate:** B-2-T1 suite green and findings reviewed.
- **Unblocks:** END OF STORY SEQUENCE (and de-risks EPIC C's `ImportJobRunner` precedent for characterize-then-change).

---

### STORY B-3 — Frontend test foundation expansion

- **Persona Narrative:** As the **solo Planza developer**, I want the highest-risk frontend logic (readiness, conflicts, layout, dates, form validation, undo/redo) under test so that UI refactors (EPIC C) have a safety net.
- **Business Value:** Med (2) · **Priority Score: 3/5**.
- **Acceptance Criteria:**
  - Given the harness from A-1-T2, When `npm run test` runs, Then suites exist and pass for `eventReadiness`, `crewConflicts`, `calendarLayout`, `dateTime` utils, `DynamicEventForm` validation, and PlannerView undo/redo.
  - Given a deliberate mutation to `crewConflicts` overlap logic, When tests run, Then at least one test fails (mutation sanity check, done once manually).
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** M
- **External Dependencies:** A-1-T2 harness.
- **TD Considerations:** sets up retirement path for TD-11 (coverage threshold) — threshold proposal recorded after this story.
- **Test Data:** fixture builders for events/crew (small, in-test).
- **Idempotency:** n/a.
- **DoR Status: READY**

#### TASK B-3-T1 — Unit tests for pure utils
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** High
- **Goal:** Characterize the four pure utils — `src/utils/eventReadiness.ts` (extend A-1-T2's smoke), `crewConflicts.ts`, `calendarLayout.ts`, `dateTime.ts` — 10–15 cases each, edge cases included (DST/UTC boundaries for `dateTime`, overlapping assignments for `crewConflicts`).
- **Budget:** ≤ 12k · **TDD Order:** characterization (pass against current behavior); surprising behaviors → findings/TD, not fixes.
- **Deliverables:** four `*.test.ts` files · **Quality Gates:** green in CI; no `src/` diffs.
- **Pull Gate:** `FrontendTestHarness` snapshot valid (A-1-T2) · **Unblocks:** B-3-T2.

#### TASK B-3-T2 — `DynamicEventForm` validation tests
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** Medium (component test: RTL + form state)
- **Goal:** RTL tests for `src/components/forms/DynamicEventForm.tsx` validation: required-field errors, type coercion, role-visible field rendering (coordinates with B-1 semantics — assert *current* frontend behavior, note divergence from B-1 if any).
- **Budget:** ≤ 10k · **TDD Order:** characterization · **Deliverables:** `DynamicEventForm.test.tsx` (+ light render helpers in a shared `test-utils.tsx` — first occurrence, no premature abstraction beyond render wrapper).
- **Quality Gates:** green in CI; runtime < 30s.
- **Pull Gate:** B-3-T1 merged (shared fixtures available) · **Unblocks:** B-3-T3.

#### TASK B-3-T3 — PlannerView undo/redo tests
- **Hat:** PREPARATORY · **Model: Sonnet** · **Confidence:** Medium
- **Goal:** Test PlannerView's undo/redo state logic: action → undo restores prior state → redo reapplies; history bounds; interleaved-edit invalidation. Prefer testing the underlying reducer/hook directly if extractable *read-only*; full-component RTL only if not.
- **Budget:** ≤ 10k · **TDD Order:** characterization · **Deliverables:** `PlannerView.undoRedo.test.tsx` (or hook test) + findings.
- **Quality Gates:** green in CI; if logic is untestable without extraction, write the PREP extraction note for EPIC C (do **not** refactor here — One Hat).
- **Pull Gate:** B-3-T2 merged · **Unblocks:** END OF STORY SEQUENCE; safety net consumed by EPIC C (TD-3).

---

### STORY B-4 — API pagination on highest-volume endpoints

- **Persona Narrative:** As a **planner at a tenant with thousands of events**, I want list endpoints paginated and the app to load incrementally so that the planner stays responsive as data grows.
- **Business Value:** High (3) · **Priority Score: 4/5**.
- **Acceptance Criteria:**
  - Given `GET /api/events?limit=50&offset=0`, When called, Then ≤ 50 items return in envelope `{ data, pagination: { total, limit, offset } }`; Given no params, Then current full-list behavior is preserved until the flag flips defaults (ASM-7 coordination).
  - Given `limit > max` or negative offset, When called, Then 400 with Zod error shape (error flow).
  - Given the frontend with incremental loading ON, When the planner opens, Then first page renders and subsequent pages load without UI regression (E2E smoke).
  - Same envelope on `/api/teams` and import-record listings.
- **INVEST:** I✓ N✓ V✓ E✓ S✓ T✓ · **Size:** L
- **External Dependencies:** none (ASM-7: frontend is the only consumer).
- **TD Considerations:** closes TD-7 for the top-3 endpoints; remaining 28 `findMany` routes stay on the register with a follow-the-pattern servicing decision (Rule of Three: the helper exists; apply opportunistically).
- **Test Data:** seed ≥ 120 events across 2 tenants (verifies RLS + pagination interplay).
- **Idempotency:** n/a (reads); ordering key (`startTime,id` tiebreak) specified to make pages stable.
- **DoR Status: READY**

#### TASK B-4-T1 — Pagination contract + `/api/events`
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Build the shared pagination helper (Zod query schema `limit` (default preserves today: absent → all, capped later; max 200), `offset`; envelope type; stable ordering) and apply to `/api/events`.
- **Budget:** ≤ 12k · **TDD Order:** (1) failing supertest specs: envelope shape, page boundaries, 400s, cross-tenant isolation under pagination; (2) helper + route change; (3) refactor helper for reuse (it ships as shared from day one — designed for B-4-T2, not premature: second consumer is already scheduled).
- **Required Interfaces:** existing events route + Zod patterns; DB-in-CI (A-2-T4) for boundary tests with seeded volume.
- **Deliverables:** `backend/src/utils/pagination.ts` + tests → `/api/events` wiring → `ADR-009: Pagination convention (limit/offset + envelope; cursor deferred until proven need)`.
- **Quality Gates:** no-param behavior unchanged (snapshot); p95 measured locally with 10k seeded events noted in PR.
- **Feature Flag:** server-side defaults stay permissive; enforcement of a default limit flips with `API_DEFAULT_PAGE_LIMIT` once B-4-T3 ships.
- **Rollback Notes:** params additive — ignore/remove helper to roll back.
- **Hand-Off Artifacts:** Contract Snapshot `PaginationEnvelope` (consumed by B-4-T2/T3 and all future list endpoints).
- **Pull Gate:** A-2-T4 DB-in-CI available; ASM-7 re-confirmed (no external API consumers found in webhook/integration configs).
- **Unblocks:** B-4-T2, B-4-T3.

#### TASK B-4-T2 — Paginate `/api/teams` + import records
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** High
- **Goal:** Apply `PaginationEnvelope` to `/api/teams` (ordering: `name,id`) and the import-record listing endpoints in `routes/import.ts` (ordering: `createdAt desc,id`).
- **Budget:** ≤ 8k · **TDD Order:** failing supertest specs per endpoint (reuse B-4-T1 spec shape) → wire helper → no refactor expected (third use validates the abstraction).
- **Required Interfaces:** `PaginationEnvelope` snapshot.
- **Deliverables:** tests → route changes → runbook note.
- **Quality Gates:** no-param behavior unchanged; import-records endpoint specifically tested at 1k+ records (it is the known volume offender).
- **Rollback Notes:** additive, same as T1.
- **Pull Gate:** B-4-T1 merged; snapshot unchanged.
- **Unblocks:** B-4-T3.

#### TASK B-4-T3 — Incremental loading in `AppProvider`
- **Hat:** FEATURE · **Model: Sonnet** · **Confidence:** Medium (touches global data flow)
- **Goal:** Update `src/context/AppProvider.tsx` to fetch paginated events/teams (first page eagerly, remainder in background or on-demand), behind flag `INCREMENTAL_LOADING` default off; add the E2E smoke (planner renders page 1, loads more, counts match).
- **Budget:** ≤ 12k · **TDD Order:** (1) failing tests: provider hook returns first page then accumulates (mock fetch via harness); failing smoke; (2) implement; (3) refactor fetch logic into a `usePagedResource` hook only if events+teams duplicate it (second occurrence → extract).
- **Required Interfaces:** `PaginationEnvelope` snapshot; `FrontendTestHarness` snapshot.
- **Deliverables:** tests → provider changes → smoke test → flag docs.
- **Quality Gates:** flag-off identical behavior; no Socket.IO regression (live updates still merge into paged state — explicit test case).
- **Feature Flag:** `INCREMENTAL_LOADING` default off; flip plan in runbook.
- **Rollback Notes:** flag off.
- **Hand-Off Artifacts:** `usePagedResource` snapshot if extracted.
- **Pull Gate:** B-4-T2 merged; B-3 suites green (regression net for provider changes).
- **Unblocks:** END OF STORY SEQUENCE; EPIC B smoke complete.

---

## Future EPICs (outline only — expand after EPIC A/B retrospective)

### EPIC C — Decompose god files (PREP for all future feature velocity)
- **Objective:** Split the four oversized modules along behavioral seams, characterization-first, so future features (incl. Players) compose stages instead of growing monoliths.
- **Mode:** DELIVERY (REFACTORING/PREPARATORY hats only per task).
- **Rough stories:** C-1 Characterize + split `backend/src/import/services/ImportJobRunner.ts` (1660 ln) into fetch/normalize/dedupe/merge/provision stage modules (PREP for EPIC G — Players must reuse these stages, not clone the team path). C-2 Split `backend/src/routes/import.ts` (1352 ln) into sub-routers. C-3 Extract PlannerView (1009 ln) state/undo-redo hook (consumes B-3-T3 net + findings). C-4 Split `AdminView.tsx` (793 ln) by admin domain. Services TD-1…TD-4.

### EPIC D — Observability
- **Objective:** Make the async chain debuggable: correlation IDs across request → outbox → worker → webhook; `/metrics` golden signals; minimal runbook per subsystem.
- **Mode:** DELIVERY.
- **Rough stories:** D-1 correlation-ID propagation (middleware + outbox payload + BullMQ job data + webhook headers, fitness-function-checked). D-2 `/metrics` endpoint (latency/traffic/errors/saturation for API + queues + DLQ depth). D-3 runbooks: import pipeline, cascade, webhooks, outbox/DLQ.

### EPIC E — Dependency upgrades (risk-ordered)
- **Objective:** Modernize the toolchain without breaking the quality loop.
- **Mode:** DELIVERY.
- **Rough stories:** E-1 Vitest 3→4 (both workspaces). E-2 Vite 6→8. E-3 Prisma 5→7 — **hard dependency on A-2 complete** (migrate-managed history is the upgrade prerequisite); riskiest, own characterization pass on generated-client call sites.

### EPIC F — API contract & type hygiene
- **Objective:** Publish the implicit API contract and burn down unsafe types.
- **Mode:** DELIVERY.
- **Rough stories:** F-1 OpenAPI generation from existing Zod schemas (+ CI check that spec stays in sync). F-2 `any` cleanup (~53 instances, mostly catch clauses → `unknown` + narrowing); services TD-8. Raises priority if ASM-7 is invalidated (external consumers appear).

### EPIC G — Teams Phase 3: Players domain (feature work)
- **Objective:** Deliver the Players repository (`Player` + `PlayerTeam`, adapter `fetchPlayers`/`normalizePlayer`, `entityScope: 'players'` import path, dedup matching, `players` route/API, roster tab + athlete grid) per `docs/teams-players-repository-plan.md` §3 Phase 3 (~6–9 d per checkpoint — relative size XL).
- **Mode:** DELIVERY.
- **Sequencing constraints (hard):** AFTER A-2 (schema work needs `prisma migrate`); SHOULD follow EPIC C-1 so the players import path **reuses extracted ImportJobRunner stages rather than cloning the team path** (the checkpoint's "clone the team path" suggestion is explicitly superseded by this backlog — cloning would compound TD-1).
- **Rough stories:** G-1 Player schema + migration. G-2 TheSportsDB players adapter + normalize. G-3 players import path via shared stages. G-4 dedup matching for players. G-5 players route/API + pagination (uses `PaginationEnvelope` from day one). G-6 Roster tab + athlete grid UI.

---

## 6. Validator Summary

Run at DELIVERY level (BB v5.1 §9). **Result: PASS** — notes below.

| Check | Result | Notes |
|---|---|---|
| Dependencies form a DAG; root stories per EPIC | ✅ | Roots: A-1-T1, A-3-T1 (parallel-safe). A-2 ⊣ A-1-T3 only at A-2-T4; B-1-T2/B-4-T1 ⊣ A-2-T4; B-3 ⊣ A-1-T2; B-4-T3 ⊣ B-4-T2 + B-3. No cycles. |
| EPIC 1 is a tracer bullet | ✅ | EPIC A = thinnest end-to-end slice of the *quality infrastructure*; tracer completes at A-2-T4 (commit → CI → typecheck/lint/test both workspaces → migrate onto disposable Postgres → green). |
| Token budgets (≤ 15k/task) | ✅ | Max declared 12k (A-2-T2, B-1-T2, B-2-T2, B-3-T1, B-4-T1/T3). B-2-T2 carries an explicit split rule if exceeded. |
| Anti-bureaucracy test | ✅ | Doc-producing tasks (A-3) have spec ≈ output by construction; code tasks specify ≤ ~1 page for multi-hundred-LOC outputs. No task spec exceeds its expected output. |
| Every story passes DoR or HOLD | ✅ | All 7 stories READY; A-2 carries an explicit conditional HOLD trigger (ASM-4 backup capability) at its pull gate. |
| Every task declares Hat; no Two-Hats violations | ✅ | Characterization tasks are PREPARATORY with zero `src/` diffs; B-2-T2 and B-3-T3 contain explicit "do not refactor here" guards. |
| TDD order (tests before implementation) | ✅ | All FEATURE tasks order tests first; PREP characterization tasks use pass-against-current-behavior convention (documented per task); A-1-T1/A-3 are verification/doc tasks (TDD n/a, stated). |
| Glossary consistency | ✅ | Membership-not-assignment, Team-vs-CanonicalTeam, Planza-not-Sporza enforced; deprecated synonyms flagged with a no-rename TD-10. |
| ADRs for cross-cutting decisions | ✅ | ADR-001…005 backfill (A-3-T2); ADR-006 CI/pkg-mgr; ADR-007 migrations; ADR-008 cascade semantics; ADR-009 pagination. |
| Critical paths tested at all layers; contract tests + mocks for external integrations | ✅ | RBAC contract tests (B-1), supertest + DB-in-CI (A-2-T4/B-4), adapter tests exist (TheSportsDB); E2E smokes per EPIC (A-1 CI smoke; B-4-T3). |
| Schema changes: migration + rollback + flag | ✅ | A-2-T3 (additive migration, documented down-SQL, no flag needed — justified); EPIC G inherits `MigrationWorkflow`. |
| Risks ≥ Medium mitigated or accepted-with-date | ✅ | All High/Med risks carry mitigations (§1); none accepted-without-mitigation. |
| Every shortcut → TD Item; Assumptions Ledger present | ✅ | TD-1…TD-11 seeded via A-3-T3; ledger §4 with 4 HIGH-impact items flagged and pull-gated. |
| SLOs + runbook per EPIC; flags for user-facing changes; idempotency on write paths | ✅ | SLOs in A and B headers; runbooks A-2-T4 / B-headers; flags: `FIELD_VISIBILITY_ENFORCEMENT`, `CASCADE_PREVIEW_PARITY`, `INCREMENTAL_LOADING`, `API_DEFAULT_PAGE_LIMIT`; this backlog adds no new write paths (pagination/filtering are reads; migrations idempotent via `migrate deploy`). |
| Economics: no over-decomposition | ✅ | 7 stories / 18 tasks across 2 EPICs (≤ 25-story cap); B-3 tasks kept merged at file-group level; solo-dev ceremony reductions applied to doc tasks only — never to DB/security/CI tasks (Core §5.1). |

**Initial-depth rule (BB §3 R4):** 2 fully decomposed EPICs (A, B), 5 outlined (C–G). Expand C–G only after the EPIC A retrospective (Phase Summary + Architecture Memory update per Core §4.3).

**Recommended execution order:** A-1-T1 → A-1-T2 → A-1-T3 → A-1-T4 → A-2-T1 → A-2-T2 → A-2-T3 → A-2-T4 *(tracer complete)* → A-3-T1 → A-3-T2 → A-3-T3 → B-1-T1 → B-1-T2 → B-2-T1 → B-2-T2 → B-3-T1 → B-3-T2 → B-3-T3 → B-4-T1 → B-4-T2 → B-4-T3. (A-3-T1 may run any time in parallel; shown late only to keep WIP-1.)

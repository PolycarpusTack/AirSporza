import { OpsPlaceholder } from './OpsPlaceholder'

/** RIGHTS — stat tiles + rights matrix (built in EPIC C). Placeholder from A-2-T1. */
export function RightsScreen() {
  return <OpsPlaceholder tabId="rights" label="RIGHTS" />
}

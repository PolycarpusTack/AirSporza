import { OpsPlaceholder } from './OpsPlaceholder'

/** SYNC — import pipeline health + merge review (built in EPIC D). Placeholder from A-2-T1. */
export function SyncScreen() {
  return <OpsPlaceholder tabId="sync" label="SYNC" />
}

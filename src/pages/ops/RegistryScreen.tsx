import { OpsPlaceholder } from './OpsPlaceholder'

/** REGISTRY — sports CMS (built in EPIC C/D). Placeholder from A-2-T1. */
export function RegistryScreen() {
  return <OpsPlaceholder tabId="registry" label="REGISTRY" />
}

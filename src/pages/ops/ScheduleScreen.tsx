import { OpsPlaceholder } from './OpsPlaceholder'

/** SCHEDULE — dense week table (built in EPIC B, Story B-1). Placeholder from A-2-T1. */
export function ScheduleScreen() {
  return <OpsPlaceholder tabId="schedule" label="SCHEDULE" />
}
